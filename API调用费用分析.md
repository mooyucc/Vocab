# Deepseek API 调用费用分析

## 价格信息（根据附图）

- **输入token（缓存命中）**：0.2元/百万tokens
- **输入token（缓存未命中）**：2元/百万tokens  
- **输出token**：3元/百万tokens

**注意**：首次调用通常为缓存未命中，后续相同或相似的prompt可能命中缓存。

---

## App中的三种API调用类型

### 1. 生成单词详情 (`generateWordDetails`)

**功能**：为新单词生成定义、词性、发音、例句等信息

**输入Token估算**：
- Prompt内容（英文）：约250字符
- 单词本身：平均5-10字符
- 系统消息格式：约50 tokens
- **总计：约350-400 tokens**

**输出Token估算**：
- JSON格式响应，包含：
  - definition（中文定义）：约20-30 tokens
  - partOfSpeech（词性）：约5 tokens
  - pronunciation（音标）：约10 tokens
  - example（英文例句）：约15-25 tokens
  - exampleCn（中文翻译）：约20-30 tokens
  - JSON格式字符：约20 tokens
- **总计：约150-250 tokens**

**费用计算**：
- **缓存未命中**：
  - 输入：400 tokens × 2元/百万 = 0.0008元
  - 输出：200 tokens × 3元/百万 = 0.0006元
  - **总计：约0.0014元/次（约0.014分）**
  
- **缓存命中**：
  - 输入：400 tokens × 0.2元/百万 = 0.00008元
  - 输出：200 tokens × 3元/百万 = 0.0006元
  - **总计：约0.00068元/次（约0.0068分）**

---

### 2. 生成每日激励语 (`generateDailyMotivation`)

**功能**：生成每日学习激励语

**输入Token估算**：
- 中文prompt：约200字符 = 约200 tokens
- 英文prompt：约250字符 = 约180 tokens
- 系统消息格式：约30 tokens
- **总计：约180-230 tokens**

**输出Token估算**：
- max_tokens设置为100
- 实际输出：15-20字（中文）或15-20词（英文）
- **总计：约50-80 tokens**

**费用计算**：
- **缓存未命中**：
  - 输入：200 tokens × 2元/百万 = 0.0004元
  - 输出：70 tokens × 3元/百万 = 0.00021元
  - **总计：约0.00061元/次（约0.0061分）**
  
- **缓存命中**：
  - 输入：200 tokens × 0.2元/百万 = 0.00004元
  - 输出：70 tokens × 3元/百万 = 0.00021元
  - **总计：约0.00025元/次（约0.0025分）**

---

### 3. 生成新例句 (`generateNewExample`)

**功能**：为已有单词生成新的例句

**输入Token估算**：
- 中文prompt：包含单词、词性、定义、当前例句等
- 基础prompt：约300字符 = 约300 tokens
- 单词信息：约50 tokens
- 当前例句（如果有）：约30-50 tokens
- **总计：约350-500 tokens**

**输出Token估算**：
- JSON格式响应，包含：
  - example（英文例句）：约20-30 tokens
  - exampleCn（中文翻译）：约20-30 tokens
  - JSON格式字符：约15 tokens
- **总计：约100-200 tokens**

**费用计算**：
- **缓存未命中**：
  - 输入：450 tokens × 2元/百万 = 0.0009元
  - 输出：150 tokens × 3元/百万 = 0.00045元
  - **总计：约0.00135元/次（约0.0135分）**
  
- **缓存命中**：
  - 输入：450 tokens × 0.2元/百万 = 0.00009元
  - 输出：150 tokens × 3元/百万 = 0.00045元
  - **总计：约0.00054元/次（约0.0054分）**

---

## 费用汇总表

| API调用类型 | 输入Token | 输出Token | 缓存未命中费用 | 缓存命中费用 |
|------------|-----------|-----------|---------------|-------------|
| 生成单词详情 | 350-400 | 150-250 | **0.0014元** | **0.00068元** |
| 生成每日激励语 | 180-230 | 50-80 | **0.00061元** | **0.00025元** |
| 生成新例句 | 350-500 | 100-200 | **0.00135元** | **0.00054元** |

---

## 实际使用场景费用估算

### 场景1：用户添加10个新单词
- 调用次数：10次 `generateWordDetails`
- **缓存未命中**：10 × 0.0014元 = **0.014元**
- **缓存命中**：10 × 0.00068元 = **0.0068元**

### 场景2：用户每天打开App（生成激励语）
- 调用次数：1次 `generateDailyMotivation`
- **缓存未命中**：**0.00061元**
- **缓存命中**：**0.00025元**

### 场景3：用户为5个单词生成新例句
- 调用次数：5次 `generateNewExample`
- **缓存未命中**：5 × 0.00135元 = **0.00675元**
- **缓存命中**：5 × 0.00054元 = **0.0027元**

---

## 成本优化建议

1. **缓存利用**：相同或相似的prompt会命中缓存，输入费用降低90%（从2元/百万降至0.2元/百万）
2. **批量处理**：考虑批量生成单词详情，减少API调用次数
3. **Token监控**：建议在代码中添加token使用统计，监控实际消耗

---

## 注意事项

1. **Token估算基于经验值**，实际token数量可能因以下因素变化：
   - 单词长度和复杂度
   - 生成内容的详细程度
   - 中英文混合比例

2. **缓存命中率**取决于：
   - Prompt的相似度
   - Deepseek的缓存策略
   - 调用频率

3. **实际费用**建议通过API响应的`usage`字段获取准确的token数量：
   ```json
   {
     "usage": {
       "prompt_tokens": 350,
       "completion_tokens": 200,
       "total_tokens": 550
     }
   }
   ```
